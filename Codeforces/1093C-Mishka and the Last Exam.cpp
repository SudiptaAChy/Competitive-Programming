#include<bits/stdc++.h>
#define ll long long
#define mx 1000009
using namespace std;
ll a[mx];
main()
{
    ll n,i,j,mxi=0;
    scanf("%lld",&n);
    for(i=0;i<n/2;i++)
    {
        scanf("%lld",&a[i]);
        mxi=max(mxi,a[i]);
    }
    mxi/=2;
    for(i=0;i<n/2;i++)
    {

    }
}
